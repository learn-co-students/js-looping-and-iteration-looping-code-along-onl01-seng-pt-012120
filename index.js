// Code your solutions in this file

// writeCards()
// returns an array of thank you messages for each name provided to the function:


  const cards = (["Ada", "Brendan", "Ali"], "birthday");
 cards.concat;
 cards.pop();

function writeCards(cards) 
{
const newmessage = [];

  for (let i = 0; i < cards.length; i++) {
    console.log(`Thank you, ${cards[i]}, for the wonderful surprise gift!`);
    newmessage.push(`Thank you, ${cards[i]}, for the wonderful surprise gift!`);
  }
 
  return newmessage;
}
 writeCards;

// 2) index.js
//        countDown()
//          logs each number when counting down, starting from the number provided:
  

function countDown() {
for (var  countDown = 10; countDown >= 0; countDown--) {
  console.log(countDown);
}
}
countDown();

